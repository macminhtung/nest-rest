import { ERoleName } from '@/common/enums';

export const DEFAULT_ROLES = {
  ADMIN: { id: 1, name: ERoleName.ADMIN },
  STAFF: { id: 2, name: ERoleName.STAFF },
  USER: { id: 3, name: ERoleName.USER },
};
