export enum ETableName {
  GROUP = 'group',
  USER = 'user',
  ROLE = 'role',
  PRODUCT = 'product',
  USER_TOKEN = 'user_token',
  GROUP_MEMBER = 'group_member',
  INVITE_MEMBER = 'invite_member',
}
